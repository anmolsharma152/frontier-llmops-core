# ─── API Keys ────────────────────────────────────────────────────────────────
# Required by: Phases 1, 4 (data generation + LLM-as-a-judge)
OPENAI_API_KEY=

# Required by: Phase 2 (optional — experiment tracking)
WANDB_API_KEY=
WANDB_PROJECT=frontier-llmops-core

# Required by: Phase 6 (guardrails — optional, Presidio PII scanning)
# PRESIDIO_API_KEY=

# ─── Redis (optional) ─────────────────────────────────────────────────────────
# Required by: Phase 6 (semantic cache — falls back to in-memory if absent)
REDIS_URL=redis://localhost:6379/0

# ─── NVIDIA NIM (alternative for data generation) ─────────────────────────────
# NVIDIA_API_KEY=
